name: cmake-build
on:
  push:
    tags:
      - 'v*'
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: build
        run: |
          mkdir build/
          cd build/
          cmake ..
          make