name: cmake-build
on:
  push:
    tags:
      - 'v*'
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: build
        run: |
          mkdir build/
          dir
          cmake -S . -B build/ -G"MinGW Makefiles" ..
          cmake --build build/